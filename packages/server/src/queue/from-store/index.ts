import { IGUHealthServerCTX } from "../../fhir-server/types.js";

// function pushFromStore(ctx: IGUHealthServerCTX) {
//   const sequenceLock = await ctx.lock?.get(ctx, "queue" ["push-from-store"]);
//   ctx.store.fhir.getSequence();
// }
