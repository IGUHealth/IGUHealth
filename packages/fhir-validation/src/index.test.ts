import { StructureDefinition, code } from "@iguhealth/fhir-types/r4/types";
import { expect, test } from "@jest/globals";
import { loadArtifacts } from "@iguhealth/artifacts";

test("Generate test validation on test data", async () => {});
